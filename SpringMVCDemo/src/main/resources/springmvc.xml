<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="
        http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/mvc
        http://www.springframework.org/schema/mvc/spring-mvc.xsd
        http://www.springframework.org/schema/context
        http://www.springframework.org/schema/context/spring-context.xsd">

    <!-- 1 开启注解扫描 -->
    <context:component-scan base-package="zy.springmvc"/>

    <!-- 2 视图解析器对象，例如本程序中sayHello方法返回了success后，会传给这个视图解析器这个对象，跳转到配置的路径下带有success的文件-->
    <bean id="internalResourceViewResolver" class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <property name="prefix" value="/WEB-INF/pages/"/>
        <property name="suffix" value=".jsp"/>
    </bean>

    <!--配置自定义类型转换器，这个是Spring提供的一个类-->
    <!--注册一个组件-->
    <bean id="conversionService" class="org.springframework.context.support.ConversionServiceFactoryBean">
        <property name="converters">
            <set>
                <bean class="zy.springmvc.utils.String2DateConvertor"/>
            </set>
        </property>
    </bean>


    <!-- 3 开启SpringMVC框架注解的支持，如@RequestMapping(path="/hello"),如果没有配置视图解析器和处理器映射器和处理器适配器，只配置这个就相当于走的默认方式-->
    <!--conversion-service表示注册的组件加载id的配置-->
    <mvc:annotation-driven conversion-service="conversionService"/>

</beans>